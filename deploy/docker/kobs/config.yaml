clusters:
  providers:
    - provider: kubeconfig
      kubeconfig:
        path: ${HOME}/.kube/config
  defaultDatasources:
    metrics: prometheus
    logs: elasticsearch
    traces: jaeger

datasources:
  - name: prometheus
    type: prometheus
    prometheus:
      address: http://localhost:9090
